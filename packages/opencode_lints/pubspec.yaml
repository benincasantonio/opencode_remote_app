name: opencode_lints
description: Custom lint rules for the OpenCode Remote App.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.11.1

dependencies:
  analyzer: ^8.1.1
  analyzer_plugin: ^0.13.0
  collection: ^1.19.1
  custom_lint_builder: ^0.8.1
  path: ^1.9.0

dev_dependencies:
  custom_lint: ^0.8.1
